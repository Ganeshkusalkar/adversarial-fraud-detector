[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    requires_torch: marks tests that require PyTorch to be installed (skip if not available)
    integration: marks tests as integration tests (may require running services)
    slow: marks tests as slow-running (skip with -m "not slow")
