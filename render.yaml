services:
  - type: web
    name: fraud-detector-api
    env: docker
    dockerfilePath: ./Dockerfile
    region: ohio
    plan: free
    port: 8000
    envVars:
      - key: PORT
        value: 8000
